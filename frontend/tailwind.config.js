/** @type {import('tailwindcss').Config} */
// NOTE: palette + typography theme is finalized from the design spec (visual system).
// This is a minimal working config so the scaffold compiles.
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
